/**
 * Temporary shim during the `@kanabun/testing` migration: the mock moved to
 * `packages/testing/src/dom-mock.ts`. Import from `@kanabun/testing` instead;
 * this file is deleted once every spec has switched over.
 */
export * from "@kanabun/testing";
